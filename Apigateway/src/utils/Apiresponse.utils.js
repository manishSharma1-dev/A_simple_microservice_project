class Apiresponse {
    constructor(
        status,
        message,
        data 
    ) {
        this.status = status ?? status
        this.message = message ?? message
        this.data = data ?? data
    }
}

export { 
    Apiresponse
}